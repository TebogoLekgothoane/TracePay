# TRACEPAY Backend

FastAPI backend scaffold for transaction ingestion, analysis, leak detection, notifications, and enterprise features.

Run locally with:

```bash
uvicorn app.main:app --reload
```
