# Copy this file to .env in the same folder (mobile/.env)
# Then restart Expo: npx expo start

# Backend API (voice chat, analyze, open banking)
# - Same machine: http://localhost:8001
# - Android emulator: http://10.0.2.2:8001
# - Physical device (same Wi‑Fi): http://YOUR_PC_IP:8001
EXPO_PUBLIC_BACKEND_URL=http://localhost:8001

# Voice chat uses Groq via the backend – put your key in web/backend/.env as GROQ_API_KEY
# (Do not put Groq API key here; the app calls the backend which uses the key on the server.)

# Optional: override Supabase (defaults are in lib/supabase.ts)
# EXPO_PUBLIC_SUPABASE_URL=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
